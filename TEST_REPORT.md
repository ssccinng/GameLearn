# GameLearn 验证报告

验证日期：2026-09-24。环境：Windows x64、.NET 10 SDK 10.0.400、OBS 32.0.4；桌面集成检查在 125% 缩放下执行。

## 2026-09-25 悬浮窗直接显示画面单词

展开的悬浮窗改为固定 **560×340**，在控制栏下直接显示每条识别原句及可点击英文词，不依赖当前选中的单行。手动识别结果直接更新悬浮窗，点词才打开释义页。保留 64×64 折叠按钮、操作保护及上一版 AI 配置修复。

**20 项悬浮界面回归通过**，报告位于 `artifacts/floating-words-final/floating-results.json`，包括：

- 两条原句同时显示，非当前选中行的单词可直接点击。
- 在空词区首次点击开启自动模式时，立即执行一次识别，不必先移开鼠标才能看到第一帧单词。
- 点击选择该词真正所属的原句，并保存展示中的截图。
- 后台新结果到来时，正在操作的词区保持不变，结束操作后只应用最新结果。
- 已失效的旧按钮不能将单词写入另一个新场景。
- 数字-only OCR 和未识别状态给出清晰提示。
- 原有按钮、自动开关、不激活窗口样式、折叠和紧凑释义页继续通过检查。

WPF 渲染预览为 `artifacts/floating-words-final/floating-words.png`；测试使用隔离数据、未显示的窗口和固定测试文本，没有操纵正在运行的用户界面。

## 2026-09-25 AI 配置保存后不可用

现场确认配置文件已成功保存、密钥可解密、模型存在于用户配置服务的模型列表。原因是旧代码拒绝非本机 HTTP 地址，并把只有主机/端口的地址拼接成 `/chat/completions`，而用户服务实际需要 `/v1/chat/completions`。

修复包括 HTTP/HTTPS 支持、根地址自动补 `/v1`、完整接口地址不重复拼接，以及独立的 AI 保存/连接测试。表单旁持续显示保存及测试状态；变更配置取消旧请求，旧回复不会覆盖新模型的结果。未填写可选 AI 时，查词会显示可操作的提示而不是抛出未处理异常。

**28 项控制台测试通过**（`artifacts/ai-unit-results.json`）；**11 项设置/运行状态检查通过**（`artifacts/ai-settings-regression/ai-settings-results.json`），覆盖无效其他表单不阻止 AI 保存、加密持久化、错误输入不覆盖原配置、运行时即时更新、旧回复丢弃及连接测试不读取解释缓存。界面检查在未显示的窗口中运行，使用隔离数据目录。

使用修复后的实际 C# 服务代码和用户已保存的配置，真实连接测试成功：模型 `gpt-6-luna`，实际请求路径 `/v1/chat/completions`，耗时约 **3.0 秒**。测试只发送固定的简短测试消息，不发送截图、学习记录或对话历史；报告 `artifacts/saved-ai-test/saved-ai-test.json` 不含密钥。运行中的用户配置文件未被诊断程序重写。

新版在 `publish/GameLearn-ai-fix`，包含之前的悬浮栏、OBS 直接捕获和操作保护。

## 2026-09-25 悬浮栏

新增可拖动、折叠、置顶、隐藏到托盘的悬浮栏，提供识别、自动开关、场景回忆和展开主窗口入口。悬浮模式采用紧凑的双页查词窗口，并继续使用既有操作保护。

**25 项控制台测试通过**（`artifacts/floating-unit-results.json`），新增位置约束测试覆盖负坐标显示器、显示器断开和屏幕边缘。

**10 项悬浮界面检查通过**（`artifacts/floating-regression-final/floating-results.json`）：

- 展开/折叠尺寸分别为 560×86 和 64×64；置顶且不显示单独任务栏图标。
- 真实但未显示的 HWND 具有 `WS_EX_NOACTIVATE`、`WS_EX_TOOLWINDOW` 样式，`ShowActivated` 关闭。
- 识别、回忆、展开、隐藏四个按钮正确分派动作；自动开关双向绑定到同一个 ViewModel / Scheduler。
- 折叠状态持久化，展开恢复完整布局。
- 紧凑查词窗口提供取词和释义/场景两页，无需打开完整主界面。
- 后台渲染不会遗留交互锁。

悬浮栏、折叠按钮、紧凑取词页和释义页已用 WPF 渲染并目视检查，图像位于 `artifacts/floating-regression-final/`。这些检查没有在用户桌面显示测试窗口或生成鼠标键盘输入；尚未逐一验证所有独占全屏游戏的焦点和置顶行为。

发行版位于 `publish/GameLearn-floating`。

## 2026-09-25 交互期间停止自动刷新

已将后台识别/记录与界面呈现分开。鼠标操作、键盘编辑、下拉选择、右键菜单、查词面板和框选/历史查看分别持有交互保护；所有保护释放后才恢复自动呈现。

**24 项控制台测试通过**（`artifacts/presentation-unit-results.json`）。另有 **16 项 WPF Dispatcher 状态回归通过**（`artifacts/presentation-regression/presentation-results.json`），使用独立数据目录和生成的固定测试图片，没有打开测试窗口或操作用户桌面：

- 操作期间截图、原句选择、词按钮、单词本和历史列表保持不变。
- 备注草稿、已掌握状态、解释和已选历史记录不被后台重写；回忆提示和自动状态信息不打断操作。
- 后台遇词记录继续保存，待呈现结果只保留最新一帧。
- 多个交互同时存在时，结束其中一个不会提前恢复刷新；结束全部交互后只刷新一次最新结果。
- 主动识别可立即更新，随后不会被更旧的缓存结果覆盖。
- 关闭自动识别、切换来源/设置时不回放过期结果。
- 未变化画面不会重建选择控件；较旧自动结果不能覆盖较新的主动预览。
- 有更新结果等待时，点词记录仍绑定正在显示的截图，而不是后台最新截图。

为避免打断当前操作，本次新版发布到 `publish/GameLearn-next`，未关闭或替换正在运行的程序。关闭当前版本后可启动该目录中的 `GameLearn.exe`；正常学习数据仍使用原本的用户数据目录。

## 自动验证

19 项控制台测试通过，最新快捷键修复报告位于 `artifacts/hotkey-fix-unit-results.json`；初版报告为 `artifacts/test-results.json`：

- PP-OCRv6 multipart 上传、模型名、Bearer 认证、任务轮询、JSONL 解析；结果资源请求不泄露令牌。
- 多边形及矩形文本框、裁剪坐标还原、无文字返回。
- 401、429、Retry-After、任务失败、未知状态、超时和取消；没有盲目重新提交。
- 自动关闭时不执行后台 OCR，仍可手动识别。
- 取消后丢弃不响应取消的 Provider 返回值；切换来源丢弃当前和排队任务。
- 连续手动请求仅保留最新一帧；未变化画面复用识别，时间仍正确更新。
- 排队的云端请求不能绕过限流；本地 Provider 不受云端限流阻塞。
- 云端 Provider 通过模拟 HTTP 分别跑通自动、手动触发。
- SQLite 持久化、词形归一、静态画面去重、上次场景、已掌握状态、删除与共享截图清理。
- 慢速 OCR 响应不等于单词消失；只有实际观察到缺席并经过十秒才判定再次出现。
- DPAPI 加密往返、快捷键格式校验、AI 解释只发送文本及语境缓存隔离。
- 单个快捷键冲突不会撤销其他有效快捷键；修改失败恢复原按键，重复组合的无效设置不会破坏已有注册。

## OBS 纯画面接入

新增 4 项 OBS 协议测试，合计 **23 项**控制台测试通过，报告为 `artifacts/obs-unit-results.json`。使用真实本机 WebSocket 模拟服务验证密码挑战响应、分片图片消息、持续连接、输入源截图、跟随当前输出场景、区域坐标、错误密码及取消后的连接丢弃。测试同时确认只发送读取请求，不混入原生窗口截图或 OBS 修改命令。

实际 OBS 32.0.4 / obs-websocket 5.6.3 验证通过：

- 最初 OBS 运行在安全模式，日志显示 `obs-websocket` 被跳过，因此 Tools 中没有服务器设置。确认未在直播或录制后，正常退出并以 `--only-bundled-plugins` 重启，开启 WebSocket，保留原密码认证和场景配置。
- 当前场景 `switch2` 的视频设备源 `ns` 返回 **1920×1080** PNG，画面完全不含 OBS 软件控件。
- 临时最小化 OBS 后仍成功读取同样尺寸的源图像，测试后恢复窗口。
- “当前输出画面”成功解析为 `switch2` 并取得场景画面。
- 本地 OCR 在实际源图像中识别到 `Valdi`、`Ah, don't get me wrong.`；本次含模型初始化的识别约 **962 ms**。

原始证据保存在 `artifacts/obs-direct-test/obs-direct-results.json`、`direct-source.png`、`source-while-minimized.png` 和 `program.png`。

更新程序时同时修复了空闲状态退出的窗口重入错误：现在等待资源释放后，将最终关闭操作排入 Dispatcher，避免在 `Closing` 事件尚未返回时再次调用 `Close`。真实空闲窗口关闭回归通过，证据为 `artifacts/close-regression/close-results.json`。

## 快捷键与捕获边框修复

现场确认 `Ctrl+Alt+E` 可注册，`Ctrl+Alt+A` 和 `Ctrl+Alt+R` 均返回 Win32 1409（已注册）。初版按整组回滚快捷键，导致识别键也被撤销。现在逐个注册，并在顶部和设置页持续显示每项实际状态；原生消息通过 WPF Dispatcher 启动异步操作，查词窗口最小化后也会恢复。

初版每次截图都创建、关闭 Windows Graphics Capture 会话，导致系统边框闪烁。现在 OBS 优先通过 `PrintWindow` 截图；其他窗口在自动模式期间复用会话，只按需读回 GPU 图像。关闭自动识别、切换来源或退出时释放会话，缩放窗口只重建帧池，并对静态窗口异步请求重绘。

13 项原生交互回归检查通过，使用真实 Windows `SendInput` 按键，报告为 `artifacts/interaction-regression-release/interaction-results.json`：

- 启动时识别快捷键独立注册成功；程序主窗口最小化、自动模式关闭时，真实 `Ctrl+Alt+E` 可识别并打开查词面板。
- 查词面板最小化后，再次按键会恢复；手动截图后不遗留捕获会话。
- 三次自动截图只创建一个会话；自动期间按快捷键获取新帧，窗口缩放后继续使用原会话。
- 关闭自动模式释放会话并停止识别。
- 另一个窗口故意占用测试快捷键时，其他快捷键仍可用；异步回调在 WPF UI 线程继续执行。
- 对正在运行的 OBS 进行连续截图，两次均使用 `PrintWindow`，没有创建 WGC 会话，因此没有该捕获方式产生的系统边框。

一般游戏窗口仍可能显示 Windows 的**稳定**捕获提示边框；本次修复消除周期性重启导致的闪烁，没有绕过系统权限来隐藏它。

## 桌面集成

12 项真实 WPF / Windows Graphics Capture 集成检查通过，报告位于 `artifacts/integration-test-2/integration-results.json`：

1. 启动时自动模式关闭。
2. 自动关闭时可捕获普通窗口并识别英文。
3. 即时识别打开可交互查词窗口。
4. `wrecked` 关联到 `wreck`，获取真实中文释义。
5. 原句保留多行游戏对话。
6. 备注、已掌握状态在列表刷新后保留。
7. 游戏筛选和单词搜索可用。
8. 开启自动模式产生识别结果。
9. 关闭自动模式后观察次数停止增长。
10. 窗口缩放后捕获宽度从 984 更新到 1184 像素。
11. 框选区域生效，关闭自动后仍能手动识别。
12. 来源最小化后报错，不生成旧画面的新记录。

已渲染并检查当前画面、单词本、设置、快捷键查词窗口。界面检查图片在 `artifacts/` 下对应的 `ui-*` 和 `integration-*` 目录中。测试数据与正常使用的数据隔离。

## OBS 真实画面

恢复游戏信号后，Windows Graphics Capture 成功获取真实画面。此前的黑屏也用两种捕获路径复核过，没有将黑图当作识别成功的游戏场景。

| 样本 | 结果 |
| --- | --- |
| 2560×1440 OBS 投影，含 `Yeah. I guess after that...seeing wrecked` | 正确识别出两行对话与角色名；预热 OCR 约 814 ms |
| 离线词典 | 770,611 条；`wrecked → wreck`，音标 `rek`，返回失事、残骸、破坏等释义 |
| OBS 主窗口十张连续真实截图 | 对话 `A little elbow grease and I could just get them back on their feet.`，140 个参考词、3 个插入错误；词级得分 `1 − WER = 97.86%` |
| 上述整窗识别 | 平均 OCR 1853.65 ms，最慢 2073.50 ms；包含 OBS 菜单和控制区的额外识别开销 |
| 对话区域裁剪 | 冷启动约 535 ms，预热 OCR 约 113 ms；正确识别两行对话 |

这些耗时是 OCR 阶段计时，不包含窗口捕获、图片编码和界面绘制。十张连续帧来自同一段对话，并非十种不同剧情或字体；加上先前截图，目前覆盖两段真实对话。不能据此推断所有游戏的准确率。

原始证据：`artifacts/real-game-samples/samples.json`、`quality.json`、`sample-01.png` 至 `sample-10.png`；其他计时见 `artifacts/obs-restored/report.json` 和 `artifacts/cropped-ui-test/report.json`。

## 尚未验证的外部条件

- 未提供官方 PP-OCRv6 访问令牌，因此未进行真实云端计费调用。协议、自动/手动组合及故障路径使用模拟 HTTP 验证，不能代替实际凭证、额度和服务可达性测试。
- 未提供 AI 翻译服务配置，语境解释以模拟服务验证；真实模型输出质量待实际服务确认。
- 未覆盖所有游戏字体、受保护画面、独占全屏及其他 DPI 比例下的实际多屏拖动。

用户可在设置中填写令牌并点击“保存并用当前区域测试官方 API”，立即完成当前机器上的实际云端连接测试。
