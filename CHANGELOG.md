# Changelog

## v0.2.10 - 发布热加载修复

- 重新打包包含翻译配置热加载修复；点击快速翻译前会读取最新本机配置。
- 检测到用户 SQLite 数据库损坏后保留原始备份，并使用完整性检查通过的恢复副本恢复词本、遇见记录、场景和最近识别。

## v0.2.9 - 翻译配置热加载

- 快速翻译点击时重新读取本机已保存的翻译配置，外部创建或更新 Azure Translator 后无需重启程序。

## v0.2.8 - 忽略 UI 后恢复刷新

- 交互保护不再因为鼠标停留在悬浮栏上永久保持；点击、滚动、拖动和输入后仍会短暂固定内容。
- 忽略 UI 文字后，保护窗口结束即可恢复最新自动识别，悬浮栏不会一直显示“内容已固定”。

## v0.2.7 - 忽略固定 UI 文字

- 主界面和悬浮栏句子卡片新增“忽略 UI”，用于隐藏 HUD、状态栏和固定菜单文字。
- 忽略签名会归一数字和标点，数值变化的同类 UI 仍会被过滤；过滤结果不会进入自动提醒或最近识别。
- 设置增加“清除忽略文字”，旧数据库自动创建忽略标记表。

## v0.2.6 - 快速句子翻译

- 释义页新增“快速翻译句子”，支持 DeepL API 和 Azure Translator，独立于 AI 语境解释。
- 翻译密钥、地址、源 / 目标语言和 Azure 区域独立配置，使用 DPAPI 保存；结果按句子缓存，默认等待上限 15 秒。
- 快速翻译失败不影响离线词典、场景截图和单词本。

## v0.2.5 - 最近识别可点词

- 最近识别记录保存 OCR 行坐标、截图和来源信息，历史句子中的英文词可直接点击查词。
- 点击历史词使用对应历史截图保存学习场景，不会误关联当前游戏画面；旧版没有截图的记录继续可读但无法回到场景。
- 增加历史记录表结构迁移和旧数据兼容。

## v0.2.4 - 单词本分类与统计

- 新增自定义分类、掌握状态与首次遇见日期筛选，支持快捷日期范围和自定义起止日期。
- 搜索扩展到原文词形、备注、分类与遇见原句；提供频次、日期和字母排序。
- 显示查词、查看、去重遇见次数及首次 / 最近遇见、最近查看时间；兼容旧库，查看统计从本版开始积累。

## v0.2.3 - 悬浮栏视觉与动效

- 重新分组主要识别操作与来源、词本、历史入口，优化间距、文字层次、渐变底色和卡片边框。
- 按钮增加短促悬停 / 按下反馈；显示、展开和收起时增加淡入滑动，尊重 Windows 关闭动画的设置。
- 识别结果自动更新时不播放入场动画，继续保留操作保护与顶部空白区域拖动。

## v0.2.2 - 本地识别提速

- 本地自动 OCR 优先处理已有文字区域，精确复用未变化区域，周期性完整扫描并在缺行、文字消失或边界风险时回退。
- 本地 OCR 直接读取拥有独立生命周期的像素快照；整窗 PNG 复用同一份编码，使用更快的无损压缩。
- OBS PrintWindow 捕获直接复制像素，移除中间 PNG 编码 / 解码。在线 OCR 仍使用原来的上传范围。
- 增加可关闭的文字区域优先选项、扫描模式状态与可重复的性能 / 自适应识别检查。

## v0.2.1 - 保留实际词形释义

- 查词详情显示游戏中实际遇到的词形，例如 `bearing`，同时标注归一后的词典词条 `bear`。
- 词形自己的音标和释义优先展示；单词本仍按词根归一去重。

## v0.2.0 - 实时选词与学习记录

- 悬浮栏支持来源选择、实时选词和最近识别；来源窗口按 OBS 投影、游戏窗口和 OBS 捕获源分组，其他来源可搜索展开。
- OBS 全屏 / 窗口投影支持按实际物理坐标叠加透明文字行；选词层不显示截图、不激活窗口，空白区域继续交给游戏操作。
- 选词目标跟随最新 OCR：同一句移动时更新坐标，句子变化或消失时撤销旧选项，点击保存最近完成识别的场景帧。
- 自动识别遇到临时捕获或本地 OCR 错误时采用退避重试，成功后自动恢复；认证错误和状态不明确的在线任务不会盲目重提。
- 单词本使用自绘圆角窗口，支持紧凑悬浮、展开阅读和返回悬浮栏；新增最近 200 次不同识别句子的本地历史，连续重复自动合并。
- 查词窗口、滚动区域和释义详情统一为应用内视觉风格；OCR 碎片按空间关系合并为更连贯的原句，并保留原始片段坐标。

## v0.1.1 - 悬浮栏学习视图

- 悬浮栏扩展为更大的学习视图，直接显示更多识别原句和可点击单词。
- 根据未收录词、未掌握词、词长、复杂词形和 OCR 置信度排序，更难的句子优先显示。
- 悬浮栏自动识别按钮改为显式即时切换；开启后没有结果时会先抓取一帧。
- 保持操作期间固定内容的行为，后台结果不会覆盖正在查看的句子或词。

## v0.1.0 - 游戏罪恶减轻器

首个可用版本：

- 支持 OBS WebSocket 直接读取捕获源、当前输出画面和 OBS 场景，不必截取 OBS 界面。
- 支持普通游戏窗口、框选识别区域、本地 PP-OCRv5 和官方 PP-OCRv6 API。
- 支持手动快捷键识别、可开关自动识别和悬浮栏模式。
- 悬浮栏直接显示当前画面原句与可点击英文单词。
- 支持离线 ECDICT 查词、词形归一、场景截图、再次遇见提醒和历史时间线。
- 支持可选 AI 语境解释，兼容 Chat Completions 地址，配置可独立保存和测试。
- 后台识别在操作期间不会替换当前画面、单词或编辑内容。
